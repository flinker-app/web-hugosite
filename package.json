{
  "name": "flinker-website",
  "private": true,
  "scripts": {
    "generate:social-images": "node scripts/generate-social-images.mjs",
    "build": "npm run generate:social-images && hugo --minify"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "sharp": "^0.35.3"
  }
}
